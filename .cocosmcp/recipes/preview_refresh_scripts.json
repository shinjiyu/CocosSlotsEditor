{
  "name": "preview_refresh_scripts",
  "description": "批量 asset-db.refresh-asset + preview.reload-terminal（默认 db://assets/scripts；可传 dbUrls）",
  "mode": "eval",
  "params": {
    "dbUrls": {
      "type": "array",
      "description": "db:// 列表；省略则 db://assets/scripts",
      "default": [],
      "optional": true
    }
  },
  "code": "const urls = (typeof dbUrls !== 'undefined' && Array.isArray(dbUrls) && dbUrls.length) ? dbUrls : ['db://assets/scripts'];\nfor (const u of urls) { await Editor.Message.request('asset-db', 'refresh-asset', u); }\nawait Editor.Message.request('preview', 'reload-terminal');\nreturn { refreshed: urls, reloaded: true };"
}
